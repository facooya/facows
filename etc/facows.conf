# Default position "/etc/facows/facows.conf"

# Default web port.
# Require http port for redirect to https.
HTTP_PORT 80
HTTPS_PORT 443

# Must not using sub domain, like "www.example.com", only using main domain.
DOMAIN "example.com"

# Default web root path for "example.com".
WEB_ROOT "/var/www/facows"

# Must not comment or delete WEB_LOG. Not support yet but using web log path in Configure structure.
WEB_LOG "/var/log/facows"

# SSL certificate and private key path. Must need to Facows.
SSL_CERT "/etc/ssl/certs/cert.pem"
SSL_KEY "/etc/ssl/private/key.pem"

# [HSTS]
# Strict https connection.
# Warnning, client only access https during seconds.
# If HSTS true, must remove comment to HSTS_MAX_AGE.
HSTS false

# [HSTS.require]
# Strict https connection during seconds.
# Value 0 HSTS initialization but https connect your site once.
# init: 0, 1d: 86400, 1m: 2592000, 1y: 31536000
# HSTS_MAX_AGE 86400

# [NFT]
# Net filter table enable for dos and flood attack.
# If NFT true, must remove comment to PPS_LIMIT, PPS_BURST, BAN_TIME.
NFT false

# [NFT.require]
# PPS: Packet per second
# Limit packet per second by one IP.
# PPS_LIMIT 1000

# [NFT.require]
# Burst packet for first page loading. Dynamic limit.
# PPS_BURST 2000

# [NFT.require]
# Default time format minute for ban.
# BAN_TIME 15

# [NFT.optional]
# More allow ports, optional if nft true. E.g., ALLOW_PORTS [PORT], [PORT], ...
# ALLOW_PORTS 80, 443
